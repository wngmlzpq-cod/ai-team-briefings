# AI Team Repository Common Rules

## 1. 기본 작업 위치

모든 작업은 다음 저장소에서 수행한다.

`C:\Users\user\AI-Team\ai-team-briefings`

## 2. Git 작업 책임

Git pull, add, commit, push는 PowerShell 실행 스크립트가 담당한다.

Claude 에이전트는 직접 Git 명령을 실행하지 않는다. Claude는 지정된 결과 파일만 생성하거나 수정한다.

## 3. 에이전트별 작업 폴더

각 에이전트는 자신에게 지정된 폴더에서만 결과 파일을 생성하거나 수정한다.

- 채용 분석: `recruitment/`
- 청년 지원: `youth-support/`
- 경제 연구: `economy/`
- 학습: `learning/`
- 검증: `qa/`

## 4. QA 에이전트 권한

QA 에이전트는 모든 에이전트 폴더의 결과물을 읽을 수 있다.

다만 QA 결과 파일의 생성과 수정은 `qa/` 폴더에서만 수행한다.

## 5. 보고서 파일명

일일 보고서는 오늘 날짜를 사용하여 다음 형식으로 작성한다.

`YYYY-MM-DD.md`

## 6. 다른 에이전트 결과 보호

다른 에이전트가 작성한 파일을 임의로 수정하거나 삭제하지 않는다.

## 7. 금지된 Git 명령

다음 명령은 실행하지 않는다.

- `git push --force`
- `git push -f`
- `git reset --hard`
- `git clean -fd`

## 8. 민감정보 보호

비밀번호, API 키, 인증 토큰, 주민등록번호, 계좌번호, 신청번호 등 민감정보를 저장하거나 보고서에 기록하지 않는다.

## 9. 사실 검증

출처를 직접 확인하지 못한 정보는 사실로 단정하지 않는다.

필요한 경우 다음과 같이 명시한다.

- 확인 필요
- 검증 불가
- 공식 출처 확인 필요

## 10. 작업 종료 조건

Claude 에이전트는 지정된 폴더에 결과 파일을 정상적으로 작성한 뒤 종료한다.

Git 기록과 GitHub 전송은 PowerShell 실행 스크립트가 담당한다.

---

# Learning Agent Rules

## 11. 왜 중심 학습법

학습 에이전트는 핵심 학습 주제를 다음 순서로 설명한다.

1. 왜 배우는가
2. 어떤 문제를 해결하는가
3. 핵심 원리
4. 비유
5. 실제 사용처
6. 장단점과 비교
7. 공식과 공식이 나온 이유
8. 쉬운 예제
9. 실무 예제
10. 자주 하는 실수
11. 확인 질문과 복습 퀴즈
12. 다음 개념과 연결

세부 작성 원칙은 다음 파일을 따른다.

`learning/prompts/why-first-method.md`

## 12. 학습 수준

학습 설명은 전기공학을 기초부터 배우는 성인 초급자가 이해할 수 있는 쉬운 한국어로 작성한다.

전문용어를 생략하지 말고, 전문용어 뒤에 쉬운 뜻을 함께 설명한다.

## 13. 학습 분량

매일 지나치게 많은 개념을 다루지 않는다.

여러 개념을 얕게 나열하는 것보다 핵심 개념을 깊이 이해시키는 것을 우선한다.

## 14. 학습 결과물

학습 에이전트는 다음 파일만 생성하거나 갱신한다.

`learning/YYYY-MM-DD.md`

학습 프롬프트 파일과 실행 스크립트는 일일 자동 실행 중에 수정하지 않는다.